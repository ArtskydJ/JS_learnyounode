//return a func that takes valid users and returns a func that returns tru if all supplied users exist in valid users

module.exports = function checkUsersValid(databaseOfUsers) {
	return function(usersToTest) {
		//SOLUTION
	}
}
